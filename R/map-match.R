

#' Match geospatial polygons
#'
#' Given two geospatial polygons `x` and `y`, assign the regions
#' from `y` to the regions in `x`.
#'
#' @param x,y An `sf` object of the same size.
#' @return An sf object with the geometry from `x` with rows
#'   matched from `y`.
#' @export
map_match <- function(x, y) {
  # compute
}
